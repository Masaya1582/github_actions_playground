name: Registered Environment Variables
on: push
env:
  EXAMPLE: Defined by workflow level
jobs:
  print:
    runs-on: ubuntu-latest
    env:
      USERNAME: ${{ vars.USERNAME }} # Repoに登録した環境変数の参照
    steps:
      - run: echo "${USERNAME}"

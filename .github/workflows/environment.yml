name: Secret Variables
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    env:
      PASSWORD: ${{ secrets.PASSWORD }}
    steps:
      - run: echo "${PASSWORD}" # 出力はマスクされる
      - run: echo "${PASSWORD:0:1} ${PASSWORD#?}" # 出力はマスクされない

name: Literals
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
     LITERAL_NULL: ${{ null }}
     LITERAL_BOOLEAN: ${{ true }}
     LITERAL_STRING: ${{ 'Hello!' }}
     LITERAL_OMIT_STRING: World
